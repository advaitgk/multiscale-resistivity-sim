# Core scientific stack for the Sn-Bi resistivity simulation.
# Minimum versions reflect APIs used by the code:
#   - scipy>=1.12 is required for scipy.sparse.linalg.cg(..., rtol=...)
#   - numba>=0.61 is required for Python 3.13 support
#
# Verified reproducible (bit-identical output across runs) on Python 3.13.5 with:
#   numpy==2.4.6  scipy==1.17.1  numba==0.65.1  matplotlib==3.11.0
# Exact resistivity values are reproducible within a fixed NumPy/Numba version;
# pin to the versions above for bit-identical results.
numpy>=1.24
scipy>=1.12
numba>=0.61
matplotlib>=3.7
